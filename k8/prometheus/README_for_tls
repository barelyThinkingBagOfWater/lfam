You'll have to use a certificate and the keytool (only the keytool doesn't work after tests)

it's pretty straightforward: https://prometheus.io/docs/prometheus/latest/configuration/configuration/#tls_config
